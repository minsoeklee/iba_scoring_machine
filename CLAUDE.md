# CLAUDE.md

## 대상 환경

- 이 사이트는 PC 브라우저에서만 쓰는 것을 전제로 한다. 모바일·태블릿 화면과 터치 입력은 지원하지 않는다.
- 좁은 화면용 미디어 쿼리, 터치 이벤트와 `touch-action`, "PC에서 즐겨 주세요" 같은 모바일 안내 문구, `apple-touch-icon`을 추가하지 않는다.
- 고해상도 모니터 대응(`devicePixelRatio`)과 좁은 PC 창에서 레이아웃을 한 칸으로 바꾸는 규칙은 PC 환경에 필요하므로 유지한다.
- 미니게임 입력은 마우스와 키보드만 고려한다.
